/// <reference types="vite/client" />

// True in the static GitHub Pages demo build (no backend — data lives in the
// browser). Injected by Vite `define` in vite.config.ts.
declare const __STATIC__: boolean;
